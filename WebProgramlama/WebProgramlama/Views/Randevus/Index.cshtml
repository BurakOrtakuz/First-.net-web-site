@model IEnumerable<WebProgramlama.Models.Randevu>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.randevuZamani)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.User)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.plaka)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Islem)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.randevuZamani)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.plaka)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Islem.IslemId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.randevuId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.randevuId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.randevuId">Sil</a>
            </td>
        </tr>
}
    </tbody>
</table>
